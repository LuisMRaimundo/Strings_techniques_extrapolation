# Bow-contact model report

Techniques: `sul_tasto`, `sul_ponticello`.

\[
\log R=\alpha_t+g(p),\quad Y=B\exp(\log R)
\]

- Distinct from mute submodel.
- Without β/force/velocity: latent missing covariates; wide priors; no invention from dynamics labels.
- Literature priors encode directional tendency only (`alpha_t_sul_*`), not fixed multipliers.
