# Mute model report

Technique: standard performance `con_sordino`.

- Target scientific form: \(S_{\mathrm{muted}}(f)=S_{\mathrm{ord}}(f)\,A_{m,i}(f)\).
- Phase 1 reduction: `scalar_descriptor_approximation` on EWSD log-ratio.
- Heavy practice mute: refused (`heavy_practice_mute_unsupported`).
- Violin/viola dB power proxies used only as wide log-ratio priors, not as universal constants.
